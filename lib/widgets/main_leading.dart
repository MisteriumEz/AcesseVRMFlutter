import 'package:flutter/material.dart';

class MainLeading extends StatelessWidget {
  const MainLeading({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return IconButton(
      icon: Image.asset("assets/images/LogoBrasil.png"),
      onPressed: () {},
    );
  }
}
